namespace MyStore.Products
{
    // ENUM: a set of named constant values. Safer than strings for fixed options.
    public enum ProductStatus
    {
        Draft,
        Active,
        Discontinued
    }
}
